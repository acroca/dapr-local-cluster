apiVersion: apps/v1
kind: Deployment
metadata:
  name: multi-agent-workflow-elf
  labels:
    app: multi-agent-workflow-elf
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: multi-agent-workflow-elf
  template:
    metadata:
      labels:
        app: multi-agent-workflow-elf
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "multi-agent-workflow-elf"
    spec:
      terminationGracePeriodSeconds: 0
      containers:
      - name: elf
        image: localhost:5001/multi-agent-workflow-elf:latest
        resources:
          limits:
            cpu: "0.5"
            memory: "256Mi"
          requests:
            cpu: "0.1"
            memory: "128Mi"
